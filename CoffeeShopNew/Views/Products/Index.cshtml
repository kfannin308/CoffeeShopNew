@model CoffeeShopNew.Models.Products[]
    
@{
    ViewData["Title"] = "Our Products";
}

<h1>Our Products</h1>
<div>
    @{
        foreach (var curr in Model)
        {
            <a href="/Products/Details?productId=@curr.Id">@curr.Description $@curr.Price</a><br />
        }
    }
</div>